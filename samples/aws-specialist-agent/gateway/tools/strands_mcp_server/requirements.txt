# Strands Agents documentation MCP server. mcp is pinned to the same
# version the other images use; the upstream package is pinned so the FastMCP
# settings override (host/port/stateless_http) stays on a verified release.
mcp==1.28.1
strands-agents-mcp-server==0.2.7
