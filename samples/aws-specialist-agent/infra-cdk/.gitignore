*.js
!jest.config.js
*.d.ts
node_modules

# CDK asset staging directory
.cdk.staging
cdk.out

# CDK context cache. Auto-generated by `cdk synth` (AZ lookups etc.) and embeds
# the AWS account id in its keys, so it is kept out of git. Regenerated locally
# on the next synth; safe to delete.
cdk.context.json
