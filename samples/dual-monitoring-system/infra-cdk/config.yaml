stack_name_base: prod-agent-monitoring-stack # Change this to your preferred name (max 35 chars)

# Optional: Set to automatically create an admin user and email credentials
# If not provided, you'll need to manually create users via AWS Console
admin_user_email: null # Replace with your email: admin@example.com

backend:
  pattern: strands-swarm-agent # Available patterns: strands-single-agent, langgraph-single-agent, strands-swarm-agent
  deployment_type: docker # Available deployment types: docker (default), zip
